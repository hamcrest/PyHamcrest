language: python

python:
  - 2.7
  - 3.5
  - 3.6
  - pypy2.7-6.0.0

env:
  - TOX_ENV=py27
  - TOX_ENV=py27-numpy
  - TOX_ENV=pypy
  - TOX_ENV=docs-py3
  - TOX_ENV=py35
  - TOX_ENV=py36

install:
  - pip install tox coveralls

script:
  - tox -e $TOX_ENV

after_success:
  - coveralls
